{
  "name": "wolves-services",
  "version": "1.0.0",
  "description": "Services of the Pet messenger project",
  "author": "moldvaer23",
  "license": "MIT",
  "private": false,
  "scripts": {
    "install:user": "cd user-service && yarn",
    "install:api": "cd api-service && yarn",
    "install:all": "yarn run install:user && yarn run install:api",
    "build:user": "cd user-service && yarn build",
    "build:api": "cd api-service && yarn build",
    "build:all": "yarn run build:user && yarn run build:api",
    "start:user": "cd user-service && yarn start",
    "start:api": "cd api-service && yarn start"
  }
}
